apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "castle-backend.fullname" . }}-test-connection"
  labels:
    {{- include "castle-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "castle-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
