apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shared-game-server.fullname" . }}-test-connection"
  labels:
    {{- include "shared-game-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shared-game-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
