apiVersion: v1
kind: Service
metadata:
  name: shared-game-server
  labels:
    app: shared-game-server
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    app: shared-game-server
