apiVersion: v1
kind: Service
metadata:
  name: {{ include "castle-backend.fullname" . }}
  labels:
    {{- include "castle-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "castle-backend.selectorLabels" . | nindent 4 }}
