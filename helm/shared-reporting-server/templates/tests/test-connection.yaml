apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "castle-reporting-server.fullname" . }}-test-connection"
  labels:
    {{- include "castle-reporting-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "castle-reporting-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
