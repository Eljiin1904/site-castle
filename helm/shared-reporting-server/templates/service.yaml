apiVersion: v1
kind: Service
metadata:
  name: castle-reporting-server
  labels:
    app: castle-reporting-server
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    app: castle-reporting-server
