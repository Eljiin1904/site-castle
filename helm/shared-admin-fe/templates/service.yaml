apiVersion: v1
kind: Service
metadata:
  name: {{ include "castle-admin-frontend.fullname" . }}
  labels:
    {{- include "castle-admin-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "castle-admin-frontend.selectorLabels" . | nindent 4 }}
