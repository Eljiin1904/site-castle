apiVersion: v1
kind: Service
metadata:
  name: castle-admin-frontend
  labels:
    app: castle-admin-frontend
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    app: castle-admin-frontend
