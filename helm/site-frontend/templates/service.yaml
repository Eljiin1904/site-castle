apiVersion: v1
kind: Service
metadata:
  name: castle-frontend
  labels:
    app: castle-frontend
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    app: castle-frontend
