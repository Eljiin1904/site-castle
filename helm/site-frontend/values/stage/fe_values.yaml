replicaCount: 1

environment: "staging"

image:
  app:
    repository: "183631306381.dkr.ecr.us-east-1.amazonaws.com/pidwin/castle-frontend"
    pullPolicy: Always
    tag: "latest"

runtimeConfig:
  appEnvName: "staging"
  siteUrl: "https://stage.brickrax.com"
  siteApi: "https://api.stage.brickrax.com"
  adminUrl: "https://admin.stage.brickrax.com"
  adminApi: "https://aapi.stage.brickrax.com"
  staticUrl: "https://font.brickrax.com"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  automount: true
  annotations: {}
  name: ""

podAnnotations: {}
podLabels: {}

podSecurityContext: {}
securityContext: {}

service:
  type: ClusterIP
  port: 80
  tls: []

ingress:
  enabled: false

secrets:
  app: "castle-frontend"

app:
  port: 80

resources: {}

# Default liveness and readiness probes
livenessProbe:
  httpGet:
    path: /
    port: http
  initialDelaySeconds: 30
  periodSeconds: 10

readinessProbe:
  httpGet:
    path: /
    port: http
  initialDelaySeconds: 5
  periodSeconds: 5

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80

volumes: []
volumeMounts: []

nodeSelector: {}
tolerations: []
affinity: {}

namespace: shared-admin-backend

# Explicit labels to ensure consistency
labels:
  app: castle-frontend

