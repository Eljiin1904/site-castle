apiVersion: v1
kind: Service
metadata:
  name: castle-admin-backend
  labels:
    app: castle-admin-backend
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    app: castle-admin-backend
