apiVersion: v1
kind: Service
metadata:
  name: {{ include "castle-admin-backend.fullname" . }}
  labels:
    {{- include "castle-admin-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "castle-admin-backend.selectorLabels" . | nindent 4 }}
