apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "castle-admin-backend.fullname" . }}-test-connection"
  labels:
    {{- include "castle-admin-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "castle-admin-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
